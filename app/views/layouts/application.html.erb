<!DOCTYPE html>
<html lang="fr">
  <head>
    <title><%= content_for?(:page_title) ? "#{yield(:page_title)} - NexP" : 'NexP - Build. Together.' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="NexP connecte les porteurs d'idées avec les développeurs, designers et builders qui cherchent un projet.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="sidebar" class="bg-background text-foreground">
    <% if user_signed_in? %>
      <%= render 'shared/sidebar' %>

      <nav class="top-navbar">
        <div class="flex items-center justify-between px-6 h-full">
          <button data-action="click->sidebar#toggle" class="md:hidden p-2 hover:bg-secondary transition-colors" style="border-radius: var(--radius);" aria-label="Ouvrir le menu" aria-expanded="false">
            <svg class="w-5 h-5 text-muted-foreground" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>

          <div class="md:hidden flex-1 text-center">
            <span class="logo">NexP<span class="logo-accent">.</span></span>
          </div>

          <div class="flex items-center gap-3 ml-auto">
            <!-- Barre de recherche globale -->
            <div data-controller="global-search" class="hidden md:block relative">
              <div class="relative">
                <input
                  type="text"
                  data-global-search-target="input"
                  data-action="input->global-search#search keydown->global-search#handleKeydown focus->global-search#showResults blur->global-search#hideResults"
                  placeholder="Rechercher..."
                  class="w-48 lg:w-64 pl-9 pr-4 py-2 text-sm bg-secondary/50 border border-border rounded-lg focus:outline-none focus:border-primary focus:ring-1 focus:ring-primary/30 transition-all"
                />
                <svg class="w-4 h-4 text-muted-foreground absolute left-3 top-1/2 -translate-y-1/2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                </svg>
              </div>
              <!-- Résultats de recherche -->
              <div data-global-search-target="results" class="hidden absolute top-full left-0 right-0 mt-2 bg-card border border-border rounded-lg shadow-lg overflow-hidden z-50">
                <div data-global-search-target="content" class="max-h-80 overflow-y-auto">
                  <!-- Résultats dynamiques -->
                </div>
              </div>
            </div>

            <!-- Theme Toggle -->
            <div data-controller="theme" class="flex items-center">
              <button data-action="click->theme#toggle" class="p-2 hover:bg-secondary transition-colors relative group" style="border-radius: var(--radius);" aria-label="Changer le thème">
                <!-- Sun icon (shown in dark mode) -->
                <svg data-theme-target="sunIcon" class="w-5 h-5 text-muted-foreground group-hover:text-primary transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
                </svg>
                <!-- Moon icon (shown in light mode) -->
                <svg data-theme-target="moonIcon" class="hidden w-5 h-5 text-muted-foreground group-hover:text-primary transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path>
                </svg>
              </button>
            </div>

            <!-- Notifications avec panneau déroulant -->
            <div data-controller="notifications-panel" data-notifications-panel-url-value="<%= unread_count_notifications_path %>" class="relative">
              <button data-action="click->notifications-panel#toggle" class="p-2 hover:bg-secondary transition-colors relative group" style="border-radius: var(--radius);" aria-label="Notifications">
                <svg class="w-5 h-5 text-muted-foreground group-hover:text-primary transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"></path>
                </svg>
                <!-- Badge de compteur -->
                <span data-notifications-panel-target="badge" class="hidden absolute -top-1 -right-1 bg-destructive text-destructive-foreground text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center"></span>
              </button>

              <!-- Panneau notifications déroulant -->
              <div data-notifications-panel-target="panel" class="hidden absolute right-0 top-full mt-2 w-80 bg-card border border-border rounded-lg shadow-xl z-50 overflow-hidden">
                <div class="p-3 border-b border-border flex items-center justify-between">
                  <h3 class="font-semibold text-foreground">Notifications</h3>
                  <%= link_to "Tout voir", notifications_path, class: "text-xs text-primary hover:text-primary/80" %>
                </div>
                <div data-notifications-panel-target="list" class="max-h-80 overflow-y-auto">
                  <div class="p-4 text-center text-sm text-muted-foreground">
                    Chargement...
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>

      <div class="main-content relative z-10 min-h-screen pt-16">
        <main class="p-6">
          <% if flash[:notice] %>
            <div class="flash-notice" data-controller="flash" data-flash-duration-value="4000" data-flash-type-value="notice" role="status" aria-live="polite">
              <div class="flex items-start gap-3">
                <svg class="w-5 h-5 text-success flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <p class="flex-1"><%= flash[:notice] %></p>
                <button type="button" data-action="click->flash#dismiss" class="text-muted-foreground hover:text-foreground transition-colors flex-shrink-0" aria-label="Fermer">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                  </svg>
                </button>
              </div>
            </div>
          <% end %>

          <% if flash[:alert] %>
            <div class="flash-alert" data-controller="flash" data-flash-duration-value="6000" data-flash-type-value="error" role="alert" aria-live="assertive">
              <div class="flex items-start gap-3">
                <svg class="w-5 h-5 text-destructive flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <p class="flex-1"><%= flash[:alert] %></p>
                <button type="button" data-action="click->flash#dismiss" class="text-muted-foreground hover:text-foreground transition-colors flex-shrink-0" aria-label="Fermer">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                  </svg>
                </button>
              </div>
            </div>
          <% end %>

          <%= yield %>
        </main>
      </div>
    <% else %>
      <main class="min-h-screen flex items-center justify-center relative z-10">
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
