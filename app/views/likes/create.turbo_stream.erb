<%= turbo_stream.replace "like_button_post_#{@post.id}" do %>
  <%= render 'posts/like_button', post: @post, size: local_assigns[:size] || 5, show_count_label: local_assigns[:show_count_label] || false %>
<% end %>
